
import React from 'react';

// This component is deprecated in the new DSA Detective UI.
// Keeping a hollow shell to ensure imports don't break during transition if any.

const PreferenceSelector: React.FC<any> = () => {
  return null;
};

export default PreferenceSelector;
